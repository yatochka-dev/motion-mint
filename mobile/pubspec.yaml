name: mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  connectrpc: ^1.0.0      # core runtime (connect.dart, io.dart, web.dart)
  protobuf: ^4.0.0
  fixnum:    any
  google_sign_in: ^7.1.1
  flutter_secure_storage: ^9.0.0
  riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: false
